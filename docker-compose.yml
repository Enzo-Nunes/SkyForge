services:
  db:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: skyforge
      POSTGRES_USER: skyforge
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-skyforge}
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "skyforge"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  db-api:
    build:
      context: .
      dockerfile: db-api/Dockerfile
    init: true
    restart: unless-stopped
    depends_on:
      - db
    environment:
      POSTGRES_HOST: db
      POSTGRES_DB: skyforge
      POSTGRES_USER: skyforge
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-skyforge}
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:5000/health')\" || exit 1"]
      interval: 5s
      timeout: 10s
      retries: 10
      start_period: 30s

  scraper:
    build:
      context: .
      dockerfile: scraper/Dockerfile
    init: true
    restart: unless-stopped
    depends_on:
      - db-api
    environment:
      WIKI_SCRAPE_INTERVAL: ${WIKI_SCRAPE_INTERVAL:-3600}

  calculator:
    build:
      context: .
      dockerfile: calculator/Dockerfile
    init: true
    restart: unless-stopped
    depends_on:
      - db-api
    environment:
      REFRESH_TIME: ${REFRESH_TIME:-120}

  web:
    build:
      context: .
      dockerfile: web/Dockerfile
      args:
        - VERSION=${VERSION:-1.0.0}
    init: true
    restart: unless-stopped
    ports:
      - "8145:8000"

volumes:
  postgres_data:
